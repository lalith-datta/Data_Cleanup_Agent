# Frontend production env — for Vercel (Project → Settings → Environment Variables).
# Point the UI at your deployed Render backend URL (no trailing slash).
NEXT_PUBLIC_API_URL=https://your-backend.onrender.com
